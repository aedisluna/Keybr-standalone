"use strict";

// The renderer loads the regular Keybr web app over HTTP and needs no Node
// access. Context isolation is enabled and nothing is exposed to the page on
// purpose; this file exists so we can add a safe bridge later if required.
